# GameAndAnimation (GAA)

# How to install:
$ pkg update && pkg upgrade
$ pkg install git
$ git clone https://github.com/afelfgie/GameAndAnimation
$ cd GameAndAnimation && chmod +x *
$ sh install.sh

# How to launch:
$ python2 run.py
